const panel=document.querySelector('.search-panel');const input=panel.querySelector('input');document.querySelector('.search-trigger').addEventListener('click',()=>{panel.classList.add('open');panel.setAttribute('aria-hidden','false');setTimeout(()=>input.focus(),200)});document.querySelector('.search-close').addEventListener('click',closeSearch);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeSearch();if(e.key==='/'&&document.activeElement.tagName!=='INPUT'){e.preventDefault();panel.classList.add('open');setTimeout(()=>input.focus(),200)}});function closeSearch(){panel.classList.remove('open');panel.setAttribute('aria-hidden','true')}
const toast=document.querySelector('.toast');function notify(message='Añadido a Mi lista'){toast.textContent=message;toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelectorAll('[data-list],.quick-add').forEach(btn=>btn.addEventListener('click',()=>{btn.classList.toggle('saved');notify(btn.classList.contains('saved')?'Añadido a Mi lista':'Eliminado de Mi lista')}));document.querySelectorAll('[data-play],.card-play').forEach(btn=>btn.addEventListener('click',()=>notify('Preparando reproducción HLS…')));
const rail=document.querySelector('.poster-rail');document.querySelectorAll('[data-scroll]').forEach(btn=>btn.addEventListener('click',()=>rail.scrollBy({left:Number(btn.dataset.scroll)*440,behavior:'smooth'})));
const sections=[...document.querySelectorAll('main section[id]')];const links=[...document.querySelectorAll('.desktop-nav a')];addEventListener('scroll',()=>{let current='inicio';sections.forEach(s=>{if(scrollY>=s.offsetTop-160)current=s.id});links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))},{passive:true});
